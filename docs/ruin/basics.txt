"basics";
"This is a comment";

"Ruin has optional type control built into it (supported types: float, int, string and bool)";
$float foo = 4.5; "Numbers such as 2.0 do not classify as floats";
$bool foo = True; "1 and 0 classify as booleans";

"In Ruin, log can be used in console.log's place";
log($float foo, $bool foo); "log writes multiple values to the console instead of side by side";
log('Hello, World! <css> color: red'); "log can also style messages";

"To import the DOM addon for flux using the built-in 'importScripts' function";
await importScripts('https://nexus-webdev.github.io/Ruin/addons/dom_flux.$');

"Flux is run like so:";
const data = await flux.run(`print 'Hello, World 2!'`);

"In Ruin, await can always be called in the global scope.";
await (new Promise(resolve => setTimeout(resolve, 3000));
log('Waited for 3 seconds.');
